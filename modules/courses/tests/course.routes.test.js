const request = require('supertest');
const app = require('../../../server');

describe('Courses routes', () => {
  it('GET /api/courses should respond', async () => {
    // Integration test skeleton: requires test DB + migrations
  });
});
