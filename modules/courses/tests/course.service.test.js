const courseService = require('../services/courseService');

describe('Course Service', () => {
  it('createCourse should throw when user is not instructor', async () => {
    // TODO: mock User.findById to return a non-instructor and assert error
  });
});
