const authService = require('../services/authService');

describe('Auth Service', () => {
  it('register skeleton', async () => {
    // TODO: mock User repo and assert token returned
  });

  it('login skeleton', async () => {
    // TODO: mock User repo and bcrypt
  });
});
